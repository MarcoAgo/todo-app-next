query appRegistration ($locale: I18NLocaleCode) {
    appRegistration(locale: $locale) {
        data {
            attributes {
                components {
                    ...registrationForm
                }
            }
        }
    }
}