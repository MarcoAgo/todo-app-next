query homepage($locale: I18NLocaleCode) {
  homepage(locale: $locale) {
    data {
      attributes {
        components {
          ...heroOpener
          ...titleDescriptionImage
        }
      }
    }
  }
}